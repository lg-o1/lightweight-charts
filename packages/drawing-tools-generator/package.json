{
  "name": "@tradingview/drawing-tools-generator",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "description": "Internal CLI for generating Lightweight Charts drawing tools from TDS specs.",
  "bin": {
    "toolgen": "./src/cli.mjs"
  },
  "scripts": {
    "generate": "node ./src/cli.mjs generate",
    "check": "node ./src/cli.mjs generate --check",
    "watch": "node ./src/cli.mjs generate --watch",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "yaml": "^2.6.0",
    "yargs": "^17.7.2"
  }
}
